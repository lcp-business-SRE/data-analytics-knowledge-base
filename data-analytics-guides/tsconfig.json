// tsconfig.json
{
  "extends": "@docusaurus/tsconfig",
  "compilerOptions": {
    "baseUrl": ".", // これがカレントディレクトリ（プロジェクトルート）を指しているか
    "paths": {
      "@site/*": ["./src/*"] // これが src ディレクトリを指しているか
    }
  },
  "exclude": [".docusaurus", "build"]
}